# Conexxa-carreer-Matcher
🚀 Conexxa – AI-Powered Career Matcher Conexxa is a smart web app that connects users to the most suitable companies based on their skills, interests, and career goals. Built using Python (Flask), scikit-learn (KNN), and HTML/CSS/JS, it analyzes your profile and recommends the best company matches instantly. 

# Instructions
# 1) Save `app.py` and create a `static` folder with `index.html` (contents above).
# 2) Install requirements: pip install flask scikit-learn pandas flask-cors
# 3) Run: python app.py (opens on http://127.0.0.1:5000)
# 4) Open your browser to http://127.0.0.1:5000 and test the form.


# Notes:
# - This is a minimal setup meant for local development and demo. For production, secure endpoints, validate inputs, and persist company data in a DB.
# - You can replace the sample `company_data` with your dataset and re-run.
